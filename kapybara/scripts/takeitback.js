function(C, A)
{
	var B = #s.scripts.lib()
	var chr = String(A.d)
	var str = A.s
	var counter = 0
	for (let i in str){
		if (str.charAt[i]===chr.charAt[0])
			{
				counter++
			}
	}
	
	return counter;
}
